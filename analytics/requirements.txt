# Core analytics dependencies — everything the Airflow DAGs import.
#
# Deliberately minimal. The analysis here is SQL plus arithmetic; pulling in pandas would hide
# the queries behind dataframe operations, and the queries are the part worth learning.
psycopg[binary]>=3.1,<4

# Lakehouse tier (Week 14). pyarrow writes Parquet and talks S3 natively; duckdb reads it back
# without a cluster. Neither needs a JVM, which is the whole appeal at this scale.
pyarrow>=18.0,<21
duckdb>=1.1,<2
